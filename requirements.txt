Flask
picamera2
opencv-python
PyYAML
schedule
requests
python-crontab
Pillow
# Note: ffmpeg is required for video recording in debug mode
# Install with: sudo apt-get install ffmpeg (Ubuntu/Debian) or brew install ffmpeg (macOS)
